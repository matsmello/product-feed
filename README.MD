Product Feed React Native app 

## Setup

* How to run
	* First install all dependencies using `yarn`
	* Then install the pod libraries usign `cd ios && pod install`
	* Then run `npx react-native run-ios`